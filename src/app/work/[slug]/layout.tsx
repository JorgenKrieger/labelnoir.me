// Imports
import type { FC, PropsWithChildren } from 'react';

// Layout component
const CaseStudyLayout: FC<PropsWithChildren> = ({ children }) => {
	return <>{children}</>;
};

// Exports
export default CaseStudyLayout;
