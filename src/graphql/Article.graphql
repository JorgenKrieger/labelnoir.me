query Article($slug: String!) {
	article(filter: {slug: {eq: $slug}}) {
		_seoMetaTags {
			attributes
			content
			tag
		}
		title
		slug
		content {
			...Code
			...Text
			...Quote
			...Media
		}
		featureImage {
			...SRCImage
		}
	}
}

